#ifndef STRING_UTILS_H
#define STRING_UTILS_H

// Retorna o tamanho da string
long long my_strlen(const char *str);

// Inverte a string in-place (no próprio buffer)
void my_reverse(char *str);

#endif
